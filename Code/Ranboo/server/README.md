# Eben Server
This is the part of Project Eben's desktop and web applications that runs on the Eben hardware. The device implements an HTTP API running at port 8000 to interface with clients. 

## Running the server
Make sure you are running as the user account which will be used by the user. Your current working directory should be where the server files are. Then, run main.py with python3.